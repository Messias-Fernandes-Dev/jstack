const {printName, lastName} = require('./printName')

printName(`Messias ${lastName}`)

// Modulos são conjustos de código

// 3 tipos de modulos 
 // -> TODOS OS ARQUIVOS JAVASCRIPT SÃO MÓDULOS;
 // -> NATIVOS - HTTP - V8 - DNS
 // -> npm (Node Package Manager);

 

